/*
 * FilterComponent Messages
 *
 * This contains all the text for the FilterComponent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FilterComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FilterComponent container!',
  },
});
