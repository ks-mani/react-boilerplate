/*
 * LabelContainer Messages
 *
 * This contains all the text for the LabelContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LabelContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LabelContainer container!',
  },
});
